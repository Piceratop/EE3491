#include <stdio.h>

typedef struct {
    double real;
    double imaginary;
} Complex;

int main() {
    double a, b, c;
    printf("Enter the coefficients of the quadratic variables\n");
    printf("(Enter a, b, c of ax^2+bx+c = 0, each seperated by a space.):\n");
    scanf("%lf %lf %lf")
}